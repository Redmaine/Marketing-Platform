# =============================================================================
# YCA Marketing Operations — environment variables
# =============================================================================

# ---- FRONTEND (set these in Netlify → Site settings → Environment variables) ----
# Safe to expose in the browser. ALWAYS the legacy JWT anon key (eyJ... format).
VITE_SUPABASE_URL=https://fvyvtdwsomxfkpxwygpk.supabase.co
VITE_SUPABASE_ANON_KEY=eyJ...   # legacy anon key — NEVER an sb_publishable_ key

# ---- EDGE FUNCTIONS ONLY (set in Supabase → Project Settings → Edge Functions
#      secrets, or `supabase secrets set`). NEVER put these in the frontend. ----
SUPABASE_SERVICE_ROLE_KEY=        # Supabase service role key
ANTHROPIC_API_KEY=                # claude-haiku-4-5-20251001 — Edge Functions only
BUFFER_ACCESS_TOKEN=              # Buffer API — Edge Functions only
GOOGLE_MY_BUSINESS_API_KEY=       # Google My Business API — Edge Functions only
RESEND_API_KEY=                   # Resend — Edge Functions only
RESEND_FROM_EMAIL=hello@yourcompanyai.co.uk
DIGEST_RECIPIENT_EMAIL=adrianfielding@me.com
